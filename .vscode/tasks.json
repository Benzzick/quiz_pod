{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker_compose_up",
            "type": "shell",
            "command": "docker compose up -d",
            "options": {
                "cwd": "${workspaceFolder}/quiz_pod_server"
            }
        }
    ]
}
